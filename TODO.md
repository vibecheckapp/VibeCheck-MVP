- [ ] Update `lib/useSpotifyPlayer.ts` with robust Spotify 403 error parsing/mapping and clearer user-facing messages.
- [ ] Update `components/RoomClient.tsx` to gracefully handle Spotify auto-play 403 errors (reduce noisy console errors, keep round flow intact, show user hint).
- [ ] Add favicon to remove `/favicon.ico` 404.
- [ ] Run a quick sanity check (type/lint-level) via targeted build command.
